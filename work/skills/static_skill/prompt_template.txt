/static_skill

请对下面的 Simulink 模型执行静态审计：

- 模型路径：{model_path}
- 额外要求：{extra_requirements}

请重点完成以下工作：

1. 命名规范检查
- 检查块名、信号线名、子系统名、Stateflow 图名是否符合 `^[A-Za-z][A-Za-z0-9_]*$`
- 标记占位命名、过于泛化的命名和明显不适合交付的默认名称

2. 连线完整性检查
- 检查是否存在悬空信号线
- 检查输入端口是否有有效上游连接
- 检查输出端口是否有有效下游连接

3. 层级结构检查
- 统计子系统数量、顶层模块数量、最大层级深度
- 识别游离模块和层级过深问题

4. Model Advisor 检查
- 执行默认检查集
- 记录失败项或执行异常

5. 输出要求
- 先返回 JSON 结果摘要，包含 `success`、`model`、`model_name`、`issues_count`、`report_path`、`summary`
- 再返回 HTML 报告路径
- 如果发现问题，请按命名、连线、层级、Model Advisor 分类说明

请通过实际脚本执行分析，不要臆造结果。若模型路径无效或脚本失败，请返回结构化错误信息。